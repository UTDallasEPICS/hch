DATABASE_URL=file:./dev.db

# Port the app server listens on, for both dev and production. Defaults to 3000.
PORT=3000

INITIAL_ADMIN_EMAIL=alice@a.com,djanjanam@gmail.com

BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
EMAIL_USER=
EMAIL_PASS=
# Dev only: without true + credentials above, OTP is printed in the terminal instead of sent.
EMAIL_OTP_USE_SMTP=true
