/** Mirrors server labels; used on the client dashboard and calendar. */
export const VIDEO_PROVIDER_LABEL: Record<string, string> = {
  GOOGLE_MEET: 'Google Meet',
  ZOOM: 'Zoom',
  OTHER: 'Video link',
}
